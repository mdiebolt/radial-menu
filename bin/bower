#!/bin/bash

set -e

git pull

VERSION=`echo "console.log(require('./package').version)" | node`
bin/update_version $VERSION

bin/prepublish

git commit -am "Version $VERSION"
git tag -a "v$VERSION" -m "Version $VERSION"
git push
git push --tags
